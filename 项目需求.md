1. 项目定位
   项目目标：开发一款即时聊天软件，实现用户间的实时消息交互、好友管理和群组互动。

核心需求：支持用户注册、登录、好友管理、群组管理、消息收发（含文本、媒体和文件）、聊天记录查询及消息撤回，同时通过 WebSocket 实现实时消息推送。

2. 业务模块及功能
   用户管理
   注册

请求参数：昵称、密码、角色（默认 USER）

返回：系统生成的用户 ID、密码等信息

登录

请求参数：用户 ID、密码

返回：Token、RefreshToken、用户信息（如昵称、头像、角色等）

Token 刷新

请求参数：RefreshToken

返回：新生成的 Token

获取用户信息

通过 JWT Token（Authorization 头）验证，返回用户详情

好友管理
添加好友

请求参数：接收方用户 ID、状态（SENT）

返回：添加好友成功提示

移除好友

请求参数：接收方用户 ID、状态（DELETED）

返回：删除好友成功提示

获取好友列表

请求参数：当前用户 ID、分页参数（页码、每页数量）

返回：好友总数及列表数据（用户 ID、昵称等）

群组管理
创建群组

请求参数：群组名称、群主用户 ID、状态（ACTIVE）

返回：群组创建成功提示

加入群组

请求参数：群组 ID、用户 ID、用户状态（MEMBER 或 ADMIN）

返回：添加成员成功提示

移除群组成员

请求参数：群组 ID、用户 ID、状态（REMOVED）

返回：移除成员成功提示

设置群组公告

请求参数：群组 ID、公告内容、状态（ACTIVE）

返回：公告更新成功提示

获取群组成员

请求参数：群组 ID、分页参数（页码、每页数量）

返回：成员总数及成员列表（用户 ID、昵称等）

消息管理
发送消息

类型：文本消息（TEXT）、媒体消息（MEDIA）、文件消息（FILE）

请求参数：消息类型、内容、接收方用户 ID、状态（SENT、DELIVERED、READ）

返回：消息发送成功提示

获取聊天记录

请求参数：发送方用户 ID、接收方用户 ID、分页参数（页码、每页记录数）

返回：总记录数及消息列表（包含消息 ID、类型、内容、发送/接收用户、时间戳等）

撤回消息

请求参数：消息 ID、发送方用户 ID、状态（REVOKED）

返回：消息撤回成功提示

WebSocket 推送
连接要求

地址：/ws

协议：STOMP over WebSocket

消息推送

客户端订阅路径：/topic/chat/{receiverId}

消息格式：包含消息类型、内容、发送者/接收者 ID、状态、时间戳等

服务器通过特定 API（如：webSocketService.sendToUser）推送实时消息

3. 技术实现与架构设计
   前后端分离

客户端：使用 Jetpack Compose 构建 UI，包含登录、注册、聊天、好友、群组、设置等多个界面

服务端：基于 RESTful API 设计（参考 API 设计文档），利用 Retrofit 调用接口

数据持久化

本地数据：采用 Room 数据库存储用户、好友、群组、消息等数据

缓存策略：支持离线缓存和数据同步

依赖注入与模块化

使用 Hilt 进行依赖注入，统一管理数据库、网络请求、WebSocket 等资源

按照模块划分代码目录（data、repository、model、viewmodel、ui、websocket、di、utils）

版本及依赖管理

利用 libs.versions.toml 统一管理依赖版本，避免导包不全和版本冲突问题

交互体验

实现用户注册、登录、好友添加、群组创建等全流程交互

提供即时通讯与实时消息推送，确保用户体验流畅

4. 功能闭环
   用户注册与登录：确保新用户能够顺利注册，并使用正确的身份验证登录，进而获取 Token 用于后续 API 调用。

消息交互：通过 RESTful API 与 WebSocket 实现消息的发送、接收、撤回以及聊天记录查询。

数据同步：本地缓存和服务端数据保持一致，确保用户在离线时也能查看历史记录。

实时互动：借助 WebSocket 推送实现即时消息和状态更新，提升互动体验。

